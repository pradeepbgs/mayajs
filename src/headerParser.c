#include <stdio.h>
#include <stdlib.h>
#include <string.h>

const char* parse_headers(const char *string) {
    if (string == NULL) {
        return "{\"error\":\"Received NULL pointer\"}";
    }
    return "p";
}
